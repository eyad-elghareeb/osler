// runner.rs — Currently a placeholder. The build/start runner state lives in
// commands.rs (shared static) for simplicity. This module exists so future
// refactors can move runner state here without touching the module tree.
