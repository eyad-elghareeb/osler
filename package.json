{
  "name": "osler",
  "version": "1.0.0",
  "private": true,
  "description": "Osler V1 — Medical study platform",
  "type": "module",
  "scripts": {
    "build": "node src/build.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "validate": "node scripts/validate-content.js",
    "validate-schemas": "node scripts/validate-schemas.js",
    "check": "npm run build && npm test && npm run validate",
    "export-schemas": "node scripts/export-schemas.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "ajv": "^8.20.0",
    "ajv-cli": "^5.0.0",
    "ajv-formats": "^3.0.1",
    "esbuild": "^0.24.0",
    "fake-indexeddb": "^6.2.5",
    "happy-dom": "^20.10.6",
    "jsdom": "^29.1.1",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "firebase": "^12.15.0"
  }
}
