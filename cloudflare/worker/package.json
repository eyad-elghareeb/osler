{
  "name": "osler-cloud-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "db:migrate": "wrangler d1 migrations apply osler-cloud --remote",
    "db:migrate:local": "wrangler d1 migrations apply osler-cloud --local",
    "db:shard": "node scripts/shard-d1.mjs",
    "db:list": "wrangler d1 migrations list osler-cloud",
    "tail": "wrangler tail",
    "secret:list": "wrangler secret list",
    "seed-admin": "npx tsx seed-admin.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^5.20260902.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.128.0"
  },
  "dependencies": {
    "unpdf": "^1.8.1"
  }
}
