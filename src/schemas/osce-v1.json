{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://osler.app/schemas/osce-v1.json",
  "title": "OSCE Simulation Content",
  "type": "object",
  "required": ["meta", "type", "stations"],
  "properties": {
    "meta": {
      "type": "object",
      "required": ["uid", "title", "schemaVersion", "createdAt", "updatedAt"],
      "properties": {
        "uid": { "type": "string" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "icon": { "type": "string" },
        "tags": { "type": "array", "items": { "type": "string" } },
        "schemaVersion": { "type": "string", "pattern": "^\\d+\\.\\d+$" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      }
    },
    "type": { "type": "string", "pattern": "^osce$" },
    "stations": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "scenario"],
        "properties": {
          "id": { "type": "string" },
          "scenario": { "type": "string" },
          "redFlags": { "type": "array", "items": { "type": "string" } },
          "differential": { "type": "array", "items": { "type": "string" } },
          "rubric": { "type": "array", "items": { "type": "string" } }
        }
      }
    }
  }
}
